apiVersion: v1
kind: Service
metadata:
  name: rta-guard
  labels:
    app: rta-guard
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: rta-guard
