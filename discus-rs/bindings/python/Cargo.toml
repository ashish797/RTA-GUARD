[package]
name = "discus-rs-py"
version = "0.1.0"
edition = "2021"
description = "Python bindings for discus-rs (RTA-GUARD)"

[lib]
name = "_native"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.28", features = ["extension-module"] }
discus-rs = { path = "../../" }
serde_json = "1"
