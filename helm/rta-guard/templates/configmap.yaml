apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rta-guard.fullname" . }}
  labels:
    {{- include "rta-guard.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
